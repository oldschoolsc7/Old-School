document.addEventListener('DOMContentLoaded', () => {document.querySelectorAll('.accordion-btn').forEach((button) => {button.addEventListener('click', () => {const content = button.nextElementSibling;const isOpen = button.classList.contains('active');document.querySelectorAll('.accordion-btn.active').forEach((other) => {if (other !== button) {other.classList.remove('active');const otherContent = other.nextElementSibling;otherContent.classList.remove('open');otherContent.style.maxHeight = null;}});button.classList.toggle('active', !isOpen);content.classList.toggle('open', !isOpen);content.style.maxHeight = !isOpen ? `${content.scrollHeight + 40}px` : null;});});});